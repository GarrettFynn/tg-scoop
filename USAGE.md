# tg-scoop 使用指南（USAGE）

面向无命令行经验的用户。所有命令以 Windows 的 **cmd.exe** 为准（开始菜单搜 `cmd` 回车）；路径示例均为占位符，请替换成你的实际路径。

目录：[这是什么](#一这是什么) · [安装](#二安装) · [快速上手（GUI）](#三快速上手gui) · [快速上手（CLI）](#四快速上手cli) · [本地密码](#五本地密码) · [输出说明](#六输出说明) · [性能与档位](#七性能与档位) · [实验性功能](#八实验性功能v02-预览) · [常见问题](#九常见问题faq) · [退出码表](#十退出码表) · [许可与免责](#十一许可与免责)

---

## 一、这是什么

tg-scoop（tg缓存捞）：**只读**提取 Telegram Desktop 本地缓存里"限制保存内容"频道的视频与图片——把**已经缓存在你自己硬盘上**的加密文件解密还原出来。

三条红线（本工具的自我约束）：

1. **只读 tdata**：绝不修改 Telegram 数据目录内的任何字节；
2. **绝不覆盖已有文件**：输出冲突自动加序号，重复运行不重复不覆盖；
3. **只提取你自己账号已缓存的数据**：不破解服务器、不绕登录、不托管内容。

## 二、安装

环境要求：Python 3.11+（Windows / macOS / Linux）。

```cmd
git clone <仓库地址> && cd tg-scoop
python -m venv .venv
.venv\Scripts\pip install -r requirements.txt
```

（macOS/Linux 把 `.venv\Scripts\` 换成 `.venv/bin/`。）

## 三、快速上手（GUI）

1. **准备**：在 Telegram Desktop 里进入受限频道，**完整播放**目标视频 / 查看目标图片；然后**完全退出** Telegram（托盘图标右键退出，不留进程）。
2. **启动**：在仓库目录运行
   ```cmd
   .venv\Scripts\python -m tg_scoop.gui
   ```
   （安装过也可用 `tg-scoop-gui` 命令。）
3. **确认 tdata 路径**：默认安装会自动填入；便携版请点"浏览"，选择 **Telegram.exe 旁边的 `tdata` 文件夹**（路径框为空时界面上会显示这条提示）。
4. **选输出目录**；（如有锁定密码）填密码——含义见[第五节](#五本地密码)。
5. **输出类型**：9 个复选框（mp4/webm/avi/mkv/mov/jpg/png/gif/webp），默认全选；至少勾一种才能开始。
6. **性能档位**：默认"均衡"（按你的 CPU/内存自动推荐进程数），一般不用动。
7. 点 **开始提取**：进度条实时走动；可随时点 **取消**（当前文件处理完后停下，已提取部分保留）。
8. 完成后弹出"是否打开输出目录"；也可以之后点 **打开目录** / **导出报告**（文本报告含统计与完整日志）。

## 四、快速上手（CLI）

最小命令（自动探测 tdata、输出到 `./tg-scoop-output`）：

```cmd
tg-scoop
```

未安装时在仓库目录用（本文其余示例同此规则）：

```cmd
.venv\Scripts\python -m tg_scoop.cli
```

常用参数（每个都可叠加）：

```cmd
:: 指定 tdata 与输出目录
tg-scoop --tdata-path "D:\path\to\tdata" --output-dir "D:\recovered"

:: tdata 设有锁定密码（省略则程序交互式询问；注意 shell 历史，见第五节）
tg-scoop --password "你的密码"

:: 只要视频
tg-scoop --types mp4,mov

:: 并行加速（N=进程数；缺省 1 串行；推荐档位见第七节）
tg-scoop --jobs 8

:: 只读分析缓存占用（不提取、不写盘）：Top 20 占空间 + 最旧 20 个 + 清理建议
tg-scoop --analyze
```

## 五、本地密码

"本地密码"指 Telegram Desktop **设置 → 隐私与安全 → 锁定密码**（锁住本机应用的那个）。**不是**登录验证码，也**不是**两步验证密码；没设过就留空。

- 密码只用于在本地派生解密密钥，不会上传或保存；
- `--password "..."` 会留在 **shell 历史与进程列表**——共享/公用机器上请省略该参数，等程序交互式询问时再输入（输入无回显，属正常）；
- 密码仅本地保存、勿外泄。

## 六、输出说明

- **命名规则**（默认路径）：`{发送者}_{时间戳}_{哈希前8位}.{扩展名}`，例如 `unknown_20260816_062209_a6d9f47c.mp4`（发送者未知时为 `unknown`；时间戳=缓存文件的修改时间；哈希=明文内容前 8 位）。
- **去重与幂等**：相同内容的文件自动识别为"重复"跳过；同名不同内容自动加 ` (1)`、` (2)` 后缀。随时可重跑做增量提取，绝不覆盖。
- **manifest.json**：每次运行在输出目录生成的清单（工具自身报告，每轮覆盖重写）。字段：

  | 字段 | 含义 |
  |---|---|
  | `version` | 结构版本（当前 2） |
  | `tdata_path` / `generated_at` | 本次的 tdata 路径 / 生成时间 |
  | `stats` | 统计：succeeded / skipped / failed / duplicates |
  | `entries` | 落盘条目：file_name、sha256、size、mtime、media_type、source_cache_dir（命中匹配时另带 `match` 子对象） |
  | `skipped_entries` | 未落盘条目及原因：`unrecognized_media_type` / `duplicate` / `filtered_by_type:{类型}` |
  | `failed_entries` | 解密失败条目与异常类型名 |
  | `match_summary` | 实验性匹配的 P1/P2/P3 计数（仅匹配运行时出现） |

- **needs-review/**：实验性匹配中"仅大小命中"（低置信）的文件落这个子目录，需人工核对。
- **.tg-scoop-state.json**：实验性匹配的断点续跑状态（已扫描聊天 + 已处理最小消息号）；删了只是下次从头扫，无妨。

## 七、性能与档位

| 档位 | 含义 |
|---|---|
| 保守 | `--jobs 1` 串行，内存占用最低 |
| 均衡（推荐） | `--jobs N`，N = min(核数-1, 8)（RAM < 4GB 时收敛到 2），GUI 缺省 |
| 极速 | `--jobs` = CPU 核数 |

- v0.1.4 起解密后端换为 C 级 AES-CTR：本机实测（约 2.8 万个缓存文件副本）串行由约 8 分 18 秒降至 **73 秒（≈6.8×）**；
- 大缓存建议直接用均衡档即可；新后端下并行相对串行差距已经很小（本机实测 73s vs 70s），串行也很够用；
- 想先看缓存占了多少空间再决定清不清：跑 `tg-scoop --analyze`（纯只读）。

## 八、实验性功能（v0.2 预览）

> **实验性，未经真实环境验证**：以下功能全部通过合成数据与 mock 测试，尚未在真实 Telegram 账号上验证过；接口与行为可能在后续版本调整。

启用条件：`pip install .[mtproto]`（可选依赖）+ 在 https://my.telegram.org 注册应用获得 `api_id` / `api_hash`（仅本地使用、勿外泄）。

```cmd
tg-scoop --chat-id <频道用户名或数字ID> --api-id <你的api_id> --api-hash "你的api_hash"
```

- **三级匹配与原始文件名**：缓存条目与聊天消息按置信度匹配（P1 精确 / P2 大小+内容头比对 / P3 仅大小）。命中后输出文件改用**原始文件名**（无原名时降级名带真实发送者）；P3 落 `needs-review/`。匹配结果标注在 manifest.json，不改默认提取行为。
- **断点续跑**：匹配中断（含限速中断）后重跑自动从上次位置继续（状态文件 `.tg-scoop-state.json`）。
- **限速与安全**：一切 API 调用走令牌桶（≤30 条/分钟硬约束）；触发 Telegram FloodWait 时按对方要求秒数暂停后安全停止（退出码 4），稍后重跑即可续跑。
- `--chat-id` 缺 api 凭据时会被忽略并给出警告（行为与不传一致）。

## 九、常见问题（FAQ）

**提示找不到缓存目录？**
先在 Telegram Desktop 里播放目标视频/查看目标图片，确认设置中没有禁用磁盘缓存，然后完全退出 Telegram 再运行。

**工具找不到 tdata / 我是便携版？**
自动探测只覆盖系统默认安装路径。便携版的 tdata 在 Telegram.exe 旁边，用 `--tdata-path` 显式指定（GUI 里手动填写或点"浏览"）。

**控制台中文乱码？**
控制台代码页问题，不影响结果。先执行 `chcp 65001` 再重跑，或重定向到文件：`... > 报告.txt 2>&1`。

**解出来的视频无法播放？**
缓存可能只包含文件的一部分（Telegram 按需缓存分片）。回频道把目标媒体**完整播放**一遍再跑；仍不行说明该分片确实不完整。

**运行时提示"检测到 Telegram Desktop 正在运行"？**
进程没退净（托盘图标右键退出，确认无残留进程）。只读操作不会损坏 tdata，但写入中的缓存可能不完整，建议完全退出后重跑。

**杀毒软件报毒？**
本工具纯本地、开源、无网络行为（实验性功能除外，且需你显式提供凭据）。如被误报，请将情况反馈到项目 Issues；白名单自行评估添加。

**重复运行会覆盖已有文件吗？**
不会。相同内容识别为重复跳过；同名不同内容自动加序号后缀。随时可重跑。

**支持第三方客户端（64Gram 等）的 tdata 吗？**
格式相同即可，用 `--tdata-path` 指向其 tdata 目录。

## 十、退出码表

| 退出码 | 含义 |
|---|---|
| 0 | 成功 |
| 1 | 系统性故障（如磁盘写失败），已中止 |
| 2 | 找不到 tdata 目录或缓存目录（或 `--types` 含未知类型） |
| 3 | 需要密码或密码错误 |
| 4 | API 限速（FloodWait），已按秒数暂停并安全停止，可稍后重跑 |

## 十一、许可与免责

MIT，见 [LICENSE](LICENSE)。本工具只做本地数据提取，不破解 Telegram 服务器、不预置代理、不托管内容；**请仅用于提取你自己账号已缓存的数据**。使用者须遵守所在司法辖区的法律以及相关内容所有者设定的规则；作者不提供任何担保，不承担任何滥用责任。
